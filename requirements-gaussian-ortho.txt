# Gaussian Splatting Orthomosaic — Tortho-Gaussian-inspired TDOM Generation
# Install in gaussian_ortho_venv

# PyTorch (installed separately via pip with CUDA index)
# torch>=2.2.0
# torchvision>=0.17.0

# 3DGS core
plyfile>=1.0
tqdm>=4.65
lpips>=0.1.4

# Image quality metrics
scikit-image>=0.21

# COLMAP sparse model loading
pycolmap>=0.6.0

# GeoTIFF output
rasterio>=1.3
numpy>=1.24
Pillow>=10.0

# Spherical Harmonics utilities (used by FAGK)
scipy>=1.11

# Testing
pytest>=7.0
