diff --git a/src/training/dataset.hpp b/src/training/dataset.hpp
index 09b42bb..c0de001 100644
--- a/src/training/dataset.hpp
+++ b/src/training/dataset.hpp
@@ -9,6 +9,7 @@
 #include "io/pipelined_image_loader.hpp"
 #include <algorithm>
 #include <chrono>
+#include <cstdlib>
 #include <condition_variable>
 #include <format>
 #include <memory>
@@ -81,8 +82,14 @@ namespace lfs::training {
     /// Random sampler that shuffles indices once and iterates through them
     class RandomSampler {
     public:
-        explicit RandomSampler(size_t size) : size_(size),
-                                              index_(0) {
+        explicit RandomSampler(size_t size)
+            : size_(size),
+              index_(0),
+              deterministic_(std::getenv("LFS_SAMPLER_SEED") != nullptr),
+              deterministic_generator_(
+                  deterministic_
+                      ? std::stoull(std::getenv("LFS_SAMPLER_SEED"))
+                      : 0ULL) {
             reset();
         }
 
@@ -94,16 +102,24 @@ namespace lfs::training {
                 size_ = *new_size;
             }
 
-            // Generate indices 0...size-1
-            indices_.resize(size_);
-            for (size_t i = 0; i < size_; ++i) {
-                indices_[i] = i;
+            // In deterministic replay mode, preserve the previous permutation
+            // between epochs. This matches a persistent std::shuffle schedule.
+            if (!deterministic_ || indices_.size() != size_) {
+                indices_.resize(size_);
+                for (size_t i = 0; i < size_; ++i) {
+                    indices_[i] = i;
+                }
             }
 
-            // Shuffle using random device
-            std::random_device rd;
-            std::mt19937 gen(rd());
-            std::shuffle(indices_.begin(), indices_.end(), gen);
+            if (deterministic_) {
+                std::shuffle(
+                    indices_.begin(), indices_.end(),
+                    deterministic_generator_);
+            } else {
+                std::random_device rd;
+                std::mt19937 gen(rd());
+                std::shuffle(indices_.begin(), indices_.end(), gen);
+            }
 
             index_ = 0;
         }
@@ -126,6 +141,8 @@ namespace lfs::training {
         size_t size_;
         size_t index_;
         std::vector<size_t> indices_;
+        bool deterministic_;
+        std::mt19937_64 deterministic_generator_;
     };
 
     /// Infinite random sampler - automatically resets when exhausted
@@ -531,6 +548,8 @@ namespace lfs::training {
               config_(config),
               mask_config_(mask_config),
               loader_(std::make_shared<lfs::io::PipelinedImageLoader>(config)),
+              deterministic_replay_(
+                  std::getenv("LFS_SAMPLER_SEED") != nullptr),
               shutdown_(false) {
 
             // Prefetch initial batch
@@ -550,7 +569,31 @@ namespace lfs::training {
             prefetch_next_batch();
 
             try {
-                auto ready = loader_->get();
+                auto take_ready = [&]() -> lfs::io::ReadyImage {
+                    if (!deterministic_replay_) {
+                        return loader_->get();
+                    }
+                    while (true) {
+                        const auto buffered =
+                            ready_by_sequence_.find(
+                                next_result_sequence_id_);
+                        if (buffered != ready_by_sequence_.end()) {
+                            auto ready = std::move(buffered->second);
+                            ready_by_sequence_.erase(buffered);
+                            ++next_result_sequence_id_;
+                            return ready;
+                        }
+                        auto ready = loader_->get();
+                        if (ready.sequence_id ==
+                            next_result_sequence_id_) {
+                            ++next_result_sequence_id_;
+                            return ready;
+                        }
+                        ready_by_sequence_.emplace(
+                            ready.sequence_id, std::move(ready));
+                    }
+                };
+                auto ready = take_ready();
                 const auto it = sequence_to_camera_.find(ready.sequence_id);
                 if (it == sequence_to_camera_.end()) {
                     LOG_ERROR("[PipelinedDataLoader] Unknown sequence_id: {}", ready.sequence_id);
@@ -585,7 +626,9 @@ namespace lfs::training {
             loader_->clear();
             sampler_.reset();
             sequence_to_camera_.clear();
+            ready_by_sequence_.clear();
             next_sequence_id_ = 0;
+            next_result_sequence_id_ = 0;
             prefetch_next_batch();
         }
 
@@ -638,7 +681,11 @@ namespace lfs::training {
         std::shared_ptr<lfs::io::PipelinedImageLoader> loader_;
 
         std::unordered_map<size_t, size_t> sequence_to_camera_;
+        std::unordered_map<size_t, lfs::io::ReadyImage>
+            ready_by_sequence_;
         size_t next_sequence_id_ = 0;
+        size_t next_result_sequence_id_ = 0;
+        bool deterministic_replay_ = false;
 
         bool shutdown_ = false;
     };
diff --git a/src/training/strategies/mrnf.cpp b/src/training/strategies/mrnf.cpp
index 8fa7261..c0de002 100644
--- a/src/training/strategies/mrnf.cpp
+++ b/src/training/strategies/mrnf.cpp
@@ -25,6 +25,22 @@
 namespace lfs::training {
 
     namespace {
+        [[nodiscard]] uint64_t replay_seed(
+            int iteration, uint64_t stream) {
+            const char* raw = std::getenv("LFS_SAMPLER_SEED");
+            if (!raw) {
+                return static_cast<uint64_t>(
+                    std::chrono::high_resolution_clock::now()
+                        .time_since_epoch()
+                        .count());
+            }
+            const auto base = std::strtoull(raw, nullptr, 10);
+            return base ^
+                   (static_cast<uint64_t>(iteration) *
+                    0x9E3779B97F4A7C15ULL) ^
+                   (stream * 0xD1B54A32D192ED03ULL);
+        }
+
         [[nodiscard]] bool mem_breakdown_enabled() {
             static const bool enabled = [] {
                 const char* raw = std::getenv("LFS_MEM_BREAKDOWN");
@@ -560,8 +576,7 @@ namespace lfs::training {
             active_mask = _free_mask.slice(0, 0, n).logical_not();
         }
 
-        auto seed = static_cast<uint64_t>(
-            std::chrono::high_resolution_clock::now().time_since_epoch().count());
+        const auto seed = replay_seed(iter, 0ULL);
 
         const auto edge_guidance = edge_guidance_factor();
 
@@ -800,15 +815,14 @@ namespace lfs::training {
             _precomputed_edge_scores = _precomputed_edge_scores.index_select(0, valid_indices).contiguous();
     }
 
-    void MRNF::inject_noise(int /*iter*/) {
+    void MRNF::inject_noise(int iter) {
         const size_t n = static_cast<size_t>(_splat_data->size());
         if (n == 0)
             return;
 
         const float lr_mean = static_cast<float>(_optimizer->get_param_lr(ParamType::Means));
 
-        auto seed = static_cast<uint64_t>(
-            std::chrono::high_resolution_clock::now().time_since_epoch().count());
+        const auto seed = replay_seed(iter, 1ULL);
 
         mrnf_strategy::launch_mrnf_noise_injection(
             _splat_data->means().ptr<float>(),
