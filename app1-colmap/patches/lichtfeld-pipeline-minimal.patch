diff --git a/CMakeLists.txt b/CMakeLists.txt
index 669ce793..2e716935 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -13,8 +13,30 @@ endif()
 
 cmake_minimum_required(VERSION 3.30)
 
+option(LFS_BUILD_HEADLESS_ONLY "Build headless training only (no GUI/visualizer)" OFF)
+option(LFS_BUILD_PIPELINE_MINIMAL "Build pipeline-minimal (headless, no USD/mesh/video/SOG)" OFF)
 option(LFS_ENFORCE_LINUX_GUI_BACKENDS "Fail configure when Linux SDL3 would lack X11 and Wayland support" ON)
 
+# Sub-options gated by LFS_BUILD_PIPELINE_MINIMAL
+option(LFS_ENABLE_USD_IO "Build USD/OpenUSD loader support" ON)
+option(LFS_ENABLE_MESH_IO "Build OpenMesh/assimp mesh loader support" ON)
+option(LFS_ENABLE_VIDEO_IO "Build FFmpeg video encoder/player support" ON)
+option(LFS_ENABLE_SOG_IO "Build SOG/SPZ/HTML export support (requires WebP + LibArchive)" ON)
+
+# Pipeline-minimal preset: headless + strip heavy IO
+if(LFS_BUILD_PIPELINE_MINIMAL)
+    set(LFS_BUILD_HEADLESS_ONLY ON CACHE BOOL "" FORCE)
+    set(LFS_ENABLE_USD_IO OFF CACHE BOOL "" FORCE)
+    set(LFS_ENABLE_MESH_IO OFF CACHE BOOL "" FORCE)
+    set(LFS_ENABLE_VIDEO_IO OFF CACHE BOOL "" FORCE)
+    set(LFS_ENABLE_SOG_IO OFF CACHE BOOL "" FORCE)
+    message(STATUS "Pipeline-minimal build: USD, mesh, video, SOG disabled")
+endif()
+
+if(LFS_BUILD_HEADLESS_ONLY)
+    set(LFS_ENFORCE_LINUX_GUI_BACKENDS OFF CACHE BOOL "" FORCE)
+endif()
+
 include("${CMAKE_CURRENT_SOURCE_DIR}/cmake/LinuxGuiSanity.cmake")
 lfs_linux_preflight_gui_backends()
 
@@ -266,25 +288,37 @@ message(STATUS "CUDA Toolkit ${CUDAToolkit_VERSION} found at ${CUDAToolkit_TARGE
 # Find packages
 find_package(TBB REQUIRED)
 find_package(Threads REQUIRED)
-find_package(OpenGL REQUIRED)
-find_package(glad REQUIRED)
-find_package(SDL3 REQUIRED)
-lfs_validate_linux_sdl_video_backends()
 find_package(glm REQUIRED)
-find_package(imgui REQUIRED)
-find_package(implot REQUIRED)
-find_package(nfd CONFIG REQUIRED)
+if(NOT LFS_BUILD_HEADLESS_ONLY)
+    find_package(OpenGL REQUIRED)
+    find_package(glad REQUIRED)
+    find_package(SDL3 REQUIRED)
+    lfs_validate_linux_sdl_video_backends()
+    find_package(imgui REQUIRED)
+    find_package(implot REQUIRED)
+    find_package(nfd CONFIG REQUIRED)
+endif()
 find_package(nlohmann_json REQUIRED)
 find_package(args REQUIRED)
 find_package(spdlog REQUIRED)
-find_package(Freetype REQUIRED)
-find_package(WebP REQUIRED)
-find_package(LibArchive REQUIRED)
+if(NOT LFS_BUILD_HEADLESS_ONLY)
+    find_package(Freetype REQUIRED)
+endif()
+if(LFS_ENABLE_SOG_IO)
+    find_package(WebP REQUIRED)
+    find_package(LibArchive REQUIRED)
+endif()
 find_package(OpenImageIO REQUIRED)
 find_package(OpenSSL REQUIRED)
-find_package(pxr CONFIG REQUIRED)
-find_package(assimp REQUIRED)
-find_package(RmlUi CONFIG REQUIRED)
+if(LFS_ENABLE_USD_IO)
+    find_package(pxr CONFIG REQUIRED)
+endif()
+if(LFS_ENABLE_MESH_IO)
+    find_package(assimp REQUIRED)
+endif()
+if(NOT LFS_BUILD_HEADLESS_ONLY)
+    find_package(RmlUi CONFIG REQUIRED)
+endif()
 
 # TORCH_CUDA_ARCH_LIST is already defined by Torch
 # use it here as the global default CUDA architecture list too
@@ -371,11 +405,13 @@ add_subdirectory(external EXCLUDE_FROM_ALL)
 add_subdirectory(src/core)       # Core module (lfs::core namespace)
 add_subdirectory(src/training)   # Training module (lfs::training namespace)
 add_subdirectory(src/io)         # I/O module (lfs::io namespace)
-add_subdirectory(src/rendering)  # Rendering module (lfs::rendering namespace)
-add_subdirectory(src/sequencer)  # Sequencer module (lfs::sequencer namespace)
-add_subdirectory(src/visualizer) # Visualizer module (lfs::vis namespace)
+if(NOT LFS_BUILD_HEADLESS_ONLY)
+    add_subdirectory(src/rendering)  # Rendering module (lfs::rendering namespace)
+    add_subdirectory(src/sequencer)  # Sequencer module (lfs::sequencer namespace)
+    add_subdirectory(src/visualizer) # Visualizer module (lfs::vis namespace)
+    add_subdirectory(src/mcp)        # MCP server (AI agent integration)
+endif()
 add_subdirectory(src/geometry)   # Geometry utilities
-add_subdirectory(src/mcp)        # MCP server (AI agent integration)
 add_subdirectory(src/python)
 
 # =============================================================================
@@ -386,18 +422,24 @@ add_subdirectory(src/python)
 # LichtFeld-Studio
 # -----------------------------------------------------------------------------
 
-add_executable(${PROJECT_NAME}
+set(LFS_APP_SOURCES
     src/app/main.cpp
     src/app/application.cpp
-    src/app/mcp_gui_tools.cpp
-    src/app/mcp_operator_tools.cpp
-    src/app/mcp_runtime_tools.cpp
-    src/app/mcp_sequencer_tools.cpp
-    src/app/mcp_ui_registry_tools.cpp
     src/app/converter.cpp
-    src/app/splash_screen.cpp
     resources/lichtfeld-studio.rc
 )
+if(NOT LFS_BUILD_HEADLESS_ONLY)
+    list(APPEND LFS_APP_SOURCES
+        src/app/mcp_runtime_tools.cpp
+        src/app/mcp_gui_tools.cpp
+        src/app/mcp_operator_tools.cpp
+        src/app/mcp_sequencer_tools.cpp
+        src/app/mcp_ui_registry_tools.cpp
+        src/app/splash_screen.cpp
+    )
+endif()
+
+add_executable(${PROJECT_NAME} ${LFS_APP_SOURCES})
 
 
 set_target_properties(${PROJECT_NAME} PROPERTIES
@@ -411,7 +453,6 @@ target_include_directories(${PROJECT_NAME}
         ${CMAKE_CURRENT_BINARY_DIR}/include
         ${CMAKE_CURRENT_SOURCE_DIR}/src/app/include  # For app headers
         ${CMAKE_CURRENT_SOURCE_DIR}/src              # For training/, visualizer/, etc.
-        ${CMAKE_CURRENT_SOURCE_DIR}/src/visualizer   # For internal visualizer headers used by GUI MCP
         ${CMAKE_CURRENT_SOURCE_DIR}/src/rendering/include  # For rendering headers (MCP GUI tools)
         ${CMAKE_CURRENT_SOURCE_DIR}/external         # For external headers
         ${CMAKE_CURRENT_SOURCE_DIR}/external/imguizmo
@@ -419,43 +460,84 @@ target_include_directories(${PROJECT_NAME}
         ${CUDAToolkit_INCLUDE_DIRS}
         ${OPENGL_INCLUDE_DIRS}
 )
-
-if(CMAKE_CXX_LINK_GROUP_USING_RESCAN_SUPPORTED)
-    set(LFS_APP_INTERNAL_LINK_LIBS
-        "$<LINK_GROUP:RESCAN,lfs_visualizer,lfs_mcp,lfs_sequencer,lfs_rendering,lfs_training,lfs_geometry,lfs_core,lfs_io,lfs_video,fastlfs_backend,lfs_python_utils>"
+if(NOT LFS_BUILD_HEADLESS_ONLY)
+    target_include_directories(${PROJECT_NAME} PRIVATE
+        ${CMAKE_CURRENT_SOURCE_DIR}/src/visualizer   # For internal visualizer headers used by GUI MCP
     )
+endif()
+
+if(LFS_BUILD_HEADLESS_ONLY)
+    if(CMAKE_CXX_LINK_GROUP_USING_RESCAN_SUPPORTED)
+        set(LFS_APP_INTERNAL_LINK_LIBS
+            "$<LINK_GROUP:RESCAN,lfs_training,lfs_geometry,lfs_core,lfs_io,fastlfs_backend,lfs_python_utils>"
+        )
+    else()
+        set(LFS_APP_INTERNAL_LINK_LIBS
+            lfs_training
+            lfs_geometry
+            lfs_core
+            lfs_io
+            fastlfs_backend
+            lfs_python_utils
+        )
+    endif()
 else()
-    set(LFS_APP_INTERNAL_LINK_LIBS
-        lfs_visualizer
-        lfs_mcp
-        lfs_sequencer
-        lfs_rendering
-        lfs_training
-        lfs_geometry
-        lfs_core
-        lfs_io
-        lfs_video
-        fastlfs_backend
-        lfs_python_utils
-    )
+    set(_gui_link_libs lfs_visualizer lfs_mcp lfs_sequencer lfs_rendering lfs_training lfs_geometry lfs_core lfs_io)
+    if(LFS_ENABLE_VIDEO_IO)
+        list(APPEND _gui_link_libs lfs_video)
+    endif()
+    list(APPEND _gui_link_libs fastlfs_backend lfs_python_utils)
+
+    if(CMAKE_CXX_LINK_GROUP_USING_RESCAN_SUPPORTED)
+        list(JOIN _gui_link_libs "," _gui_link_libs_csv)
+        set(LFS_APP_INTERNAL_LINK_LIBS
+            "$<LINK_GROUP:RESCAN,${_gui_link_libs_csv}>"
+        )
+    else()
+        set(LFS_APP_INTERNAL_LINK_LIBS ${_gui_link_libs})
+    endif()
 endif()
 
 target_link_libraries(${PROJECT_NAME}
         PRIVATE
         ${LFS_APP_INTERNAL_LINK_LIBS}
 
-        ${OPENGL_LIBRARIES}
         CUDA::cudart
         spdlog::spdlog
         taywee::args
         OpenImageIO::OpenImageIO
+)
+if(NOT LFS_BUILD_HEADLESS_ONLY)
+    target_link_libraries(${PROJECT_NAME} PRIVATE
+        ${OPENGL_LIBRARIES}
         glad::glad
         SDL3::SDL3
-)
+    )
+endif()
+
+if(LFS_BUILD_HEADLESS_ONLY)
+    target_compile_definitions(${PROJECT_NAME} PRIVATE LFS_HEADLESS_ONLY)
+endif()
+
+# Propagate IO sub-option compile definitions to the main executable
+if(NOT LFS_ENABLE_USD_IO)
+    target_compile_definitions(${PROJECT_NAME} PRIVATE LFS_WITHOUT_USD)
+endif()
+if(NOT LFS_ENABLE_MESH_IO)
+    target_compile_definitions(${PROJECT_NAME} PRIVATE LFS_WITHOUT_MESH)
+endif()
+if(NOT LFS_ENABLE_VIDEO_IO)
+    target_compile_definitions(${PROJECT_NAME} PRIVATE LFS_WITHOUT_VIDEO)
+endif()
+if(NOT LFS_ENABLE_SOG_IO)
+    target_compile_definitions(${PROJECT_NAME} PRIVATE LFS_WITHOUT_SOG)
+endif()
 
 # The app embeds Python and loads the nanobind module at runtime for panels,
 # menus, and operators. Keep the runtime artifact in lockstep with the app.
-add_dependencies(${PROJECT_NAME} lfs_py)
+if(TARGET lfs_py)
+    add_dependencies(${PROJECT_NAME} lfs_py)
+endif()
 
 # Copy nvImageCodec shared libraries next to executable for easy deployment
 if(TARGET nvimgcodec)
@@ -653,7 +735,9 @@ message(STATUS "Post-build: Resources will be copied to \${BUILD_DIR}/resources/
 
 # Platform-specific settings
 if(UNIX)
-    target_link_libraries(${PROJECT_NAME} PRIVATE GL GLU)
+    if(NOT LFS_BUILD_HEADLESS_ONLY)
+        target_link_libraries(${PROJECT_NAME} PRIVATE GL GLU)
+    endif()
 
     if(BUILD_PORTABLE)
         set_target_properties(${PROJECT_NAME} PROPERTIES
@@ -762,8 +846,10 @@ configure_build_type(fastlfs_backend)
 configure_build_type(lfs_core)
 configure_build_type(lfs_core_cuda)
 configure_build_type(lfs_training)
-configure_build_type(lfs_rendering)
-configure_build_type(lfs_visualizer)
+if(NOT LFS_BUILD_HEADLESS_ONLY)
+    configure_build_type(lfs_rendering)
+    configure_build_type(lfs_visualizer)
+endif()
 configure_build_type(lfs_io)
 configure_build_type(lfs_geometry)
 configure_build_type(${PROJECT_NAME})
@@ -833,31 +919,37 @@ install(TARGETS lfs_event_bridge
     LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR}
     RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR}
 )
-install(TARGETS lfs_visualizer
-    LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR}
-    RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR}
-)
-install(TARGETS lfs_mcp
-    LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR}
-    RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR}
-)
+if(NOT LFS_BUILD_HEADLESS_ONLY)
+    install(TARGETS lfs_visualizer
+        LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR}
+        RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR}
+    )
+    install(TARGETS lfs_mcp
+        LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR}
+        RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR}
+    )
+endif()
 install(FILES "${CMAKE_SOURCE_DIR}/LICENSE" DESTINATION ".")
 
 set(INSTALL_RESOURCE_DIR "${CMAKE_INSTALL_DATADIR}/LichtFeld-Studio")
-install(DIRECTORY "${CMAKE_SOURCE_DIR}/src/rendering/resources/shaders" DESTINATION "${INSTALL_RESOURCE_DIR}")
-install(DIRECTORY "${CMAKE_SOURCE_DIR}/src/visualizer/gui/assets/icon" DESTINATION "${INSTALL_RESOURCE_DIR}/assets")
-install(DIRECTORY "${CMAKE_SOURCE_DIR}/src/visualizer/gui/assets/fonts" DESTINATION "${INSTALL_RESOURCE_DIR}/assets")
+if(NOT LFS_BUILD_HEADLESS_ONLY)
+    install(DIRECTORY "${CMAKE_SOURCE_DIR}/src/rendering/resources/shaders" DESTINATION "${INSTALL_RESOURCE_DIR}")
+endif()
+if(NOT LFS_BUILD_HEADLESS_ONLY)
+    install(DIRECTORY "${CMAKE_SOURCE_DIR}/src/visualizer/gui/assets/icon" DESTINATION "${INSTALL_RESOURCE_DIR}/assets")
+    install(DIRECTORY "${CMAKE_SOURCE_DIR}/src/visualizer/gui/assets/fonts" DESTINATION "${INSTALL_RESOURCE_DIR}/assets")
+    install(DIRECTORY "${CMAKE_SOURCE_DIR}/src/visualizer/gui/assets/themes" DESTINATION "${INSTALL_RESOURCE_DIR}/assets")
+    install(FILES "${CMAKE_SOURCE_DIR}/src/visualizer/gui/assets/lichtfeld-icon.png" DESTINATION "${INSTALL_RESOURCE_DIR}/assets")
+    install(FILES "${CMAKE_SOURCE_DIR}/src/visualizer/gui/assets/lichtfeld-splash-logo.png" DESTINATION "${INSTALL_RESOURCE_DIR}/assets")
+    install(FILES "${CMAKE_SOURCE_DIR}/src/visualizer/gui/assets/lichtfeld-splash-logo-dark.png" DESTINATION "${INSTALL_RESOURCE_DIR}/assets")
+    install(FILES "${CMAKE_SOURCE_DIR}/src/visualizer/gui/assets/lichtfeld-splash-loading.png" DESTINATION "${INSTALL_RESOURCE_DIR}/assets")
+    install(FILES "${CMAKE_SOURCE_DIR}/src/visualizer/gui/assets/lichtfeld-splash-loading-dark.png" DESTINATION "${INSTALL_RESOURCE_DIR}/assets")
+    install(FILES "${CMAKE_SOURCE_DIR}/src/visualizer/gui/assets/core11-logo.png" DESTINATION "${INSTALL_RESOURCE_DIR}/assets")
+    install(FILES "${CMAKE_SOURCE_DIR}/src/visualizer/gui/assets/core11-logo-dark.png" DESTINATION "${INSTALL_RESOURCE_DIR}/assets")
+    install(DIRECTORY "${CMAKE_SOURCE_DIR}/src/visualizer/gui/rmlui/resources/" DESTINATION "${INSTALL_RESOURCE_DIR}/assets/rmlui")
+    install(DIRECTORY "${CMAKE_SOURCE_DIR}/src/visualizer/gui/resources/locales" DESTINATION "${INSTALL_RESOURCE_DIR}")
+endif()
 install(FILES "${CMAKE_SOURCE_DIR}/src/rendering/resources/assets/JetBrainsMono-Regular.ttf" DESTINATION "${INSTALL_RESOURCE_DIR}/assets/fonts")
-install(DIRECTORY "${CMAKE_SOURCE_DIR}/src/visualizer/gui/assets/themes" DESTINATION "${INSTALL_RESOURCE_DIR}/assets")
-install(FILES "${CMAKE_SOURCE_DIR}/src/visualizer/gui/assets/lichtfeld-icon.png" DESTINATION "${INSTALL_RESOURCE_DIR}/assets")
-install(FILES "${CMAKE_SOURCE_DIR}/src/visualizer/gui/assets/lichtfeld-splash-logo.png" DESTINATION "${INSTALL_RESOURCE_DIR}/assets")
-install(FILES "${CMAKE_SOURCE_DIR}/src/visualizer/gui/assets/lichtfeld-splash-logo-dark.png" DESTINATION "${INSTALL_RESOURCE_DIR}/assets")
-install(FILES "${CMAKE_SOURCE_DIR}/src/visualizer/gui/assets/lichtfeld-splash-loading.png" DESTINATION "${INSTALL_RESOURCE_DIR}/assets")
-install(FILES "${CMAKE_SOURCE_DIR}/src/visualizer/gui/assets/lichtfeld-splash-loading-dark.png" DESTINATION "${INSTALL_RESOURCE_DIR}/assets")
-install(FILES "${CMAKE_SOURCE_DIR}/src/visualizer/gui/assets/core11-logo.png" DESTINATION "${INSTALL_RESOURCE_DIR}/assets")
-install(FILES "${CMAKE_SOURCE_DIR}/src/visualizer/gui/assets/core11-logo-dark.png" DESTINATION "${INSTALL_RESOURCE_DIR}/assets")
-install(DIRECTORY "${CMAKE_SOURCE_DIR}/src/visualizer/gui/rmlui/resources/" DESTINATION "${INSTALL_RESOURCE_DIR}/assets/rmlui")
-install(DIRECTORY "${CMAKE_SOURCE_DIR}/src/visualizer/gui/resources/locales" DESTINATION "${INSTALL_RESOURCE_DIR}")
 
 # Plugin development documentation
 set(DOC_INSTALL_DIR "${CMAKE_INSTALL_DATADIR}/doc/LichtFeld-Studio")
diff --git a/external/CMakeLists.txt b/external/CMakeLists.txt
index 6379100a..0ec701ff 100644
--- a/external/CMakeLists.txt
+++ b/external/CMakeLists.txt
@@ -4,30 +4,36 @@
 
 # External dependencies (EXCLUDE_FROM_ALL to prevent third-party install rules)
 
-if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/imguizmo/CMakeLists.txt")
-    message(STATUS "Adding ImGuizmo")
-    add_subdirectory(imguizmo EXCLUDE_FROM_ALL)
-else()
-    message(WARNING "ImGuizmo not found - gizmo controls unavailable")
-endif()
+if(NOT LFS_BUILD_HEADLESS_ONLY)
+    if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/imguizmo/CMakeLists.txt")
+        message(STATUS "Adding ImGuizmo")
+        add_subdirectory(imguizmo EXCLUDE_FROM_ALL)
+    else()
+        message(WARNING "ImGuizmo not found - gizmo controls unavailable")
+    endif()
 
-# Zep - embeddable editor core
-if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/zep/CMakeLists.txt")
-    message(STATUS "Adding Zep from external/zep")
-    add_subdirectory(zep EXCLUDE_FROM_ALL)
-else()
-    message(WARNING "Zep not found at external/zep")
+    # Zep - embeddable editor core
+    if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/zep/CMakeLists.txt")
+        message(STATUS "Adding Zep from external/zep")
+        add_subdirectory(zep EXCLUDE_FROM_ALL)
+    else()
+        message(WARNING "Zep not found at external/zep")
+    endif()
 endif()
 
 # OpenMesh - Half-edge mesh data structure
-if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/OpenMesh/CMakeLists.txt")
-    message(STATUS "Adding OpenMesh")
-    set(OPENMESH_BUILD_SHARED OFF CACHE BOOL "" FORCE)
-    set(OPENMESH_BUILD_UNIT_TESTS OFF CACHE BOOL "" FORCE)
-    set(BUILD_TESTING OFF CACHE BOOL "" FORCE)
-    add_subdirectory(OpenMesh EXCLUDE_FROM_ALL)
+if(LFS_ENABLE_MESH_IO)
+    if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/OpenMesh/CMakeLists.txt")
+        message(STATUS "Adding OpenMesh")
+        set(OPENMESH_BUILD_SHARED OFF CACHE BOOL "" FORCE)
+        set(OPENMESH_BUILD_UNIT_TESTS OFF CACHE BOOL "" FORCE)
+        set(BUILD_TESTING OFF CACHE BOOL "" FORCE)
+        add_subdirectory(OpenMesh EXCLUDE_FROM_ALL)
+    else()
+        message(WARNING "OpenMesh not found at external/OpenMesh")
+    endif()
 else()
-    message(WARNING "OpenMesh not found at external/OpenMesh")
+    message(STATUS "Skipping OpenMesh (LFS_ENABLE_MESH_IO=OFF)")
 endif()
 
 # nvImageCodec - GPU-accelerated image codec library
diff --git a/src/app/application.cpp b/src/app/application.cpp
index b0cc5e2d..0e4f9336 100644
--- a/src/app/application.cpp
+++ b/src/app/application.cpp
@@ -3,7 +3,9 @@
  * SPDX-License-Identifier: GPL-3.0-or-later */
 
 #include "app/application.hpp"
+#ifndef LFS_HEADLESS_ONLY
 #include "app/splash_screen.hpp"
+#endif
 #include "control/command_api.hpp"
 #include "core/checkpoint_format.hpp"
 #include "core/cuda_version.hpp"
@@ -16,19 +18,27 @@
 #include "core/scene.hpp"
 #include "core/tensor.hpp"
 #include "io/cache_image_loader.hpp"
+#ifndef LFS_HEADLESS_ONLY
 #include "rendering/framebuffer_factory.hpp"
+#endif
 #include "training/trainer.hpp"
 #include "training/training_setup.hpp"
+#ifndef LFS_HEADLESS_ONLY
 #include "visualizer/visualizer.hpp"
+#endif
 
+#ifndef LFS_HEADLESS_ONLY
 #include "app/mcp_gui_tools.hpp"
 #include "io/video/video_encoder.hpp"
 #include "mcp/mcp_http_server.hpp"
 #include "mcp/mcp_tools.hpp"
+#endif
 #include "python/runner.hpp"
+#ifndef LFS_HEADLESS_ONLY
 #include "visualizer/gui/panels/python_scripts_panel.hpp"
 #include "visualizer/gui/video_widget_interface.hpp"
 #include "visualizer/gui/windows/video_extractor_dialog.hpp"
+#endif
 #include <cstdlib>
 #include <cuda_runtime.h>
 #include <rasterization_api.h>
@@ -114,7 +124,9 @@ namespace lfs::app {
 
                     if (!params->python_scripts.empty()) {
                         trainer->set_python_scripts(params->python_scripts);
+#ifndef LFS_HEADLESS_ONLY
                         vis::gui::panels::PythonScriptManagerState::getInstance().setScripts(params->python_scripts);
+#endif
                     }
 
                     if (const auto result = trainer->initialize(checkpoint_params); !result) {
@@ -158,7 +170,9 @@ namespace lfs::app {
 
                     if (!params->python_scripts.empty()) {
                         trainer->set_python_scripts(params->python_scripts);
+#ifndef LFS_HEADLESS_ONLY
                         vis::gui::panels::PythonScriptManagerState::getInstance().setScripts(params->python_scripts);
+#endif
                     }
 
                     if (const auto result = trainer->initialize(*params); !result) {
@@ -221,6 +235,7 @@ namespace lfs::app {
             fast_lfs::rasterization::warmup_kernels();
         }
 
+#ifndef LFS_HEADLESS_ONLY
         int runGui(std::unique_ptr<lfs::core::param::TrainingParameters> params) {
             if (params->optimization.no_interop) {
                 LOG_INFO("CUDA-GL interop disabled");
@@ -312,6 +327,7 @@ namespace lfs::app {
 
             std::_Exit(0);
         }
+#endif // !LFS_HEADLESS_ONLY
 
 #ifdef WIN32
         void hideConsoleWindow() {
@@ -348,11 +364,16 @@ namespace lfs::app {
             return runHeadless(std::move(params));
         }
 
+#ifndef LFS_HEADLESS_ONLY
 #ifdef WIN32
         hideConsoleWindow();
 #endif
 
         return runGui(std::move(params));
+#else
+        LOG_ERROR("GUI mode not available in headless-only build");
+        return 1;
+#endif
     }
 
 } // namespace lfs::app
diff --git a/src/app/converter.cpp b/src/app/converter.cpp
index 752c5627..a4f5e3ab 100644
--- a/src/app/converter.cpp
+++ b/src/app/converter.cpp
@@ -155,18 +155,38 @@ namespace lfs::app {
                 result = lfs::io::save_ply(*splat, {.output_path = output, .binary = true});
                 break;
             case param::OutputFormat::SOG:
+#ifndef LFS_WITHOUT_SOG
                 result = lfs::io::save_sog(*splat, {.output_path = output, .kmeans_iterations = params.sog_iterations});
+#else
+                LOG_ERROR("SOG export not available in this build");
+                return false;
+#endif
                 break;
             case param::OutputFormat::SPZ:
+#ifndef LFS_WITHOUT_SOG
                 result = lfs::io::save_spz(*splat, {.output_path = output});
+#else
+                LOG_ERROR("SPZ export not available in this build");
+                return false;
+#endif
                 break;
             case param::OutputFormat::HTML:
+#ifndef LFS_WITHOUT_SOG
                 result = lfs::io::export_html(*splat, {.output_path = output, .kmeans_iterations = params.sog_iterations});
+#else
+                LOG_ERROR("HTML export not available in this build");
+                return false;
+#endif
                 break;
             case param::OutputFormat::USD:
             case param::OutputFormat::USDA:
             case param::OutputFormat::USDC:
+#ifndef LFS_WITHOUT_USD
                 result = lfs::io::save_usd(*splat, {.output_path = output});
+#else
+                LOG_ERROR("USD export not available in this build");
+                return false;
+#endif
                 break;
             }
 
diff --git a/src/core/CMakeLists.txt b/src/core/CMakeLists.txt
index b08688c2..d9b3af43 100644
--- a/src/core/CMakeLists.txt
+++ b/src/core/CMakeLists.txt
@@ -97,9 +97,17 @@ target_link_libraries(lfs_core
         lfs_tensor_kernels
         taywee::args
         OpenImageIO::OpenImageIO
-        $<IF:$<PLATFORM_ID:Windows>,OpenMeshCore,OpenMeshCoreStatic>
 )
 
+# OpenMesh for mesh normal computation (only if mesh support is enabled)
+if(LFS_ENABLE_MESH_IO)
+    target_link_libraries(lfs_core PRIVATE
+        $<IF:$<PLATFORM_ID:Windows>,OpenMeshCore,OpenMeshCoreStatic>
+    )
+else()
+    target_compile_definitions(lfs_core PRIVATE LFS_WITHOUT_MESH)
+endif()
+
 # OpenMP for tensor multi-threaded operations
 find_package(OpenMP)
 if(OpenMP_CXX_FOUND)
diff --git a/src/core/mesh_data.cpp b/src/core/mesh_data.cpp
index b6268536..d444e0c6 100644
--- a/src/core/mesh_data.cpp
+++ b/src/core/mesh_data.cpp
@@ -3,17 +3,21 @@
  * SPDX-License-Identifier: GPL-3.0-or-later */
 
 #include "core/mesh_data.hpp"
-#include <OpenMesh/Core/Mesh/TriMesh_ArrayKernelT.hh>
 #include <cassert>
 
-namespace lfs::core {
+#ifndef LFS_WITHOUT_MESH
+#include <OpenMesh/Core/Mesh/TriMesh_ArrayKernelT.hh>
+#endif
 
-    using TriMesh = OpenMesh::TriMesh_ArrayKernelT<>;
+namespace lfs::core {
 
     void MeshData::compute_normals() {
+#ifndef LFS_WITHOUT_MESH
         assert(vertices.is_valid() && vertices.ndim() == 2 && vertices.shape()[1] == 3);
         assert(indices.is_valid() && indices.ndim() == 2 && indices.shape()[1] == 3);
 
+        using TriMesh = OpenMesh::TriMesh_ArrayKernelT<>;
+
         auto cpu_verts = vertices.to(Device::CPU).contiguous();
         auto cpu_idx = indices.to(Device::CPU).contiguous();
         const int64_t nv = vertex_count();
@@ -55,6 +59,10 @@ namespace lfs::core {
         }
 
         mark_dirty();
+#else
+        // Mesh support disabled (LFS_WITHOUT_MESH)
+        throw std::runtime_error("compute_normals() requires mesh support (OpenMesh)");
+#endif // LFS_WITHOUT_MESH
     }
 
 } // namespace lfs::core
diff --git a/src/io/CMakeLists.txt b/src/io/CMakeLists.txt
index 1721c025..ea32dcd3 100644
--- a/src/io/CMakeLists.txt
+++ b/src/io/CMakeLists.txt
@@ -52,74 +52,80 @@ if(MSVC)
 endif()
 
 # SPZ library (Niantic compressed format) - MIT licensed
-add_library(spz_lib STATIC
-        ${CMAKE_SOURCE_DIR}/external/spz/load-spz.cc
-        ${CMAKE_SOURCE_DIR}/external/spz/splat-types.cc
-        ${CMAKE_SOURCE_DIR}/external/spz/splat-c-types.cc
-)
-target_include_directories(spz_lib PUBLIC ${CMAKE_SOURCE_DIR}/external/spz)
-target_link_libraries(spz_lib PRIVATE ZLIB::ZLIB)
-target_compile_features(spz_lib PUBLIC cxx_std_17)
-set_target_properties(spz_lib PROPERTIES POSITION_INDEPENDENT_CODE ON)
+if(LFS_ENABLE_SOG_IO)
+    add_library(spz_lib STATIC
+            ${CMAKE_SOURCE_DIR}/external/spz/load-spz.cc
+            ${CMAKE_SOURCE_DIR}/external/spz/splat-types.cc
+            ${CMAKE_SOURCE_DIR}/external/spz/splat-c-types.cc
+    )
+    target_include_directories(spz_lib PUBLIC ${CMAKE_SOURCE_DIR}/external/spz)
+    target_link_libraries(spz_lib PRIVATE ZLIB::ZLIB)
+    target_compile_features(spz_lib PUBLIC cxx_std_17)
+    set_target_properties(spz_lib PROPERTIES POSITION_INDEPENDENT_CODE ON)
+endif()
 
 # Video encoding library (FFmpeg + CUDA for GPU-accelerated encoding)
-find_package(PkgConfig REQUIRED)
-pkg_check_modules(FFMPEG REQUIRED IMPORTED_TARGET
-        libavcodec
-        libavformat
-        libswscale
-        libavutil
-)
+if(LFS_ENABLE_VIDEO_IO)
+    find_package(PkgConfig REQUIRED)
+    pkg_check_modules(FFMPEG REQUIRED IMPORTED_TARGET
+            libavcodec
+            libavformat
+            libswscale
+            libavutil
+    )
 
-add_library(lfs_video STATIC
-        include/io/video/video_encoder_interface.hpp
-        include/io/video/video_export_options.hpp
-        video/video_encoder.hpp
-        video/video_encoder.cpp
-        video/color_convert.cuh
-        video/color_convert.cu
-        video_frame_extractor.hpp
-        video_frame_extractor.cpp
-        video_player.hpp
-        video_player.cpp
-)
+    add_library(lfs_video STATIC
+            include/io/video/video_encoder_interface.hpp
+            include/io/video/video_export_options.hpp
+            video/video_encoder.hpp
+            video/video_encoder.cpp
+            video/color_convert.cuh
+            video/color_convert.cu
+            video_frame_extractor.hpp
+            video_frame_extractor.cpp
+            video_player.hpp
+            video_player.cpp
+    )
 
-target_include_directories(lfs_video
-        PUBLIC
-        $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/include>
-        $<INSTALL_INTERFACE:include>
-        PRIVATE
-        ${CMAKE_CURRENT_SOURCE_DIR}
-        ${CMAKE_SOURCE_DIR}/src/core/include
-        ${CMAKE_SOURCE_DIR}/src
-        ${CMAKE_SOURCE_DIR}/external
-)
+    target_include_directories(lfs_video
+            PUBLIC
+            $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/include>
+            $<INSTALL_INTERFACE:include>
+            PRIVATE
+            ${CMAKE_CURRENT_SOURCE_DIR}
+            ${CMAKE_SOURCE_DIR}/src/core/include
+            ${CMAKE_SOURCE_DIR}/src
+            ${CMAKE_SOURCE_DIR}/external
+    )
 
-target_link_libraries(lfs_video
-        PRIVATE
-        PkgConfig::FFMPEG
-        spdlog::spdlog
-        CUDA::cudart
-)
+    target_link_libraries(lfs_video
+            PRIVATE
+            PkgConfig::FFMPEG
+            spdlog::spdlog
+            CUDA::cudart
+    )
 
-target_compile_features(lfs_video PUBLIC cxx_std_23)
+    target_compile_features(lfs_video PUBLIC cxx_std_23)
 
-set_target_properties(lfs_video PROPERTIES
-        CUDA_SEPARABLE_COMPILATION OFF
-        CUDA_STANDARD 20
-        CUDA_STANDARD_REQUIRED ON
-        POSITION_INDEPENDENT_CODE ON
-)
+    set_target_properties(lfs_video PROPERTIES
+            CUDA_SEPARABLE_COMPILATION OFF
+            CUDA_STANDARD 20
+            CUDA_STANDARD_REQUIRED ON
+            POSITION_INDEPENDENT_CODE ON
+    )
 
-target_compile_options(lfs_video PRIVATE
-        $<$<AND:$<COMPILE_LANGUAGE:CUDA>,$<CXX_COMPILER_ID:MSVC>,$<CONFIG:Debug>>:-O0 -g ${LFS_CUDA_DEVICE_DEBUG_FLAGS} --extended-lambda -Xcompiler=/Od -Xcompiler=/Z7 -Xcompiler=/utf-8>
-        $<$<AND:$<COMPILE_LANGUAGE:CUDA>,$<CXX_COMPILER_ID:MSVC>,$<CONFIG:Release>>:-O3 -use_fast_math --extended-lambda -Xcompiler=/O2 -Xcompiler=/utf-8>
-        $<$<AND:$<COMPILE_LANGUAGE:CUDA>,$<NOT:$<CXX_COMPILER_ID:MSVC>>,$<CONFIG:Debug>>:-O0 -g ${LFS_CUDA_DEVICE_DEBUG_FLAGS} --extended-lambda>
-        $<$<AND:$<COMPILE_LANGUAGE:CUDA>,$<NOT:$<CXX_COMPILER_ID:MSVC>>,$<CONFIG:Release>>:-O3 -use_fast_math --extended-lambda>
-)
+    target_compile_options(lfs_video PRIVATE
+            $<$<AND:$<COMPILE_LANGUAGE:CUDA>,$<CXX_COMPILER_ID:MSVC>,$<CONFIG:Debug>>:-O0 -g ${LFS_CUDA_DEVICE_DEBUG_FLAGS} --extended-lambda -Xcompiler=/Od -Xcompiler=/Z7 -Xcompiler=/utf-8>
+            $<$<AND:$<COMPILE_LANGUAGE:CUDA>,$<CXX_COMPILER_ID:MSVC>,$<CONFIG:Release>>:-O3 -use_fast_math --extended-lambda -Xcompiler=/O2 -Xcompiler=/utf-8>
+            $<$<AND:$<COMPILE_LANGUAGE:CUDA>,$<NOT:$<CXX_COMPILER_ID:MSVC>>,$<CONFIG:Debug>>:-O0 -g ${LFS_CUDA_DEVICE_DEBUG_FLAGS} --extended-lambda>
+            $<$<AND:$<COMPILE_LANGUAGE:CUDA>,$<NOT:$<CXX_COMPILER_ID:MSVC>>,$<CONFIG:Release>>:-O3 -use_fast_math --extended-lambda>
+    )
+else()
+    message(STATUS "Skipping lfs_video (LFS_ENABLE_VIDEO_IO=OFF)")
+endif()
 
-# Main IO library
-add_library(lfs_io STATIC
+# Main IO library - source list (conditionally populated)
+set(LFS_IO_SRCS
         # Implementation file only (header is in include/io/)
         loader.cpp
 
@@ -130,24 +136,15 @@ add_library(lfs_io STATIC
         loader_service.hpp
         loader_service.cpp
 
-        # Format implementations (load + save)
+        # Format implementations - always built
         formats/ply.hpp
         formats/ply.cpp
         formats/colmap.hpp
         formats/colmap.cpp
         formats/transforms.hpp
         formats/transforms.cpp
-        formats/sogs.hpp
-        formats/sogs.cpp
-        formats/html.hpp
-        formats/html.cpp
-        formats/html_viewer_resources.hpp
-        formats/spz.hpp
-        formats/spz.cpp
-        formats/usd.hpp
-        formats/usd.cpp
 
-        # Concrete loader implementations
+        # Concrete loader implementations - always built
         loaders/loader_utils.hpp
         loaders/ply_loader.hpp
         loaders/ply_loader.cpp
@@ -155,78 +152,136 @@ add_library(lfs_io STATIC
         loaders/colmap_loader.cpp
         loaders/blender_loader.hpp
         loaders/blender_loader.cpp
+        loaders/checkpoint_loader.hpp
+        loaders/checkpoint_loader.cpp
+
+        # Image loaders - always built
+        cache_image_loader.cpp
+        nvcodec_image_loader.hpp
+        nvcodec_image_loader.cpp
+        pipelined_image_loader.cpp
+        stb_image_impl.cpp
+        tinyply.cpp
+)
+
+# SOG/SPZ/HTML formats (require WebP + LibArchive)
+if(LFS_ENABLE_SOG_IO)
+    list(APPEND LFS_IO_SRCS
+        formats/sogs.hpp
+        formats/sogs.cpp
+        formats/html.hpp
+        formats/html.cpp
+        formats/html_viewer_resources.hpp
+        formats/spz.hpp
+        formats/spz.cpp
         loaders/sogs_loader.hpp
         loaders/sogs_loader.cpp
         loaders/spz_loader.hpp
         loaders/spz_loader.cpp
+    )
+endif()
+
+# USD support
+if(LFS_ENABLE_USD_IO)
+    list(APPEND LFS_IO_SRCS
+        formats/usd.hpp
+        formats/usd.cpp
         loaders/usd_loader.hpp
         loaders/usd_loader.cpp
-        loaders/checkpoint_loader.hpp
-        loaders/checkpoint_loader.cpp
+    )
+endif()
+
+# Mesh support (OpenMesh + assimp)
+if(LFS_ENABLE_MESH_IO)
+    list(APPEND LFS_IO_SRCS
         loaders/mesh_loader.hpp
         loaders/mesh_loader.cpp
         mesh/openmesh_bridge.hpp
         mesh/openmesh_bridge.cpp
         mesh/texture_loader.hpp
         mesh/texture_loader.cpp
-        cache_image_loader.cpp
-        nvcodec_image_loader.hpp
-        nvcodec_image_loader.cpp
-        pipelined_image_loader.cpp
-        stb_image_impl.cpp
-        tinyply.cpp
+    )
+endif()
 
-)
+add_library(lfs_io STATIC ${LFS_IO_SRCS})
 
 # Set include directories
-target_include_directories(lfs_io
-        PUBLIC
-        $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/include>
-        $<INSTALL_INTERFACE:include>
-        PRIVATE
+set(LFS_IO_PRIVATE_INCLUDE_DIRS
         ${CMAKE_SOURCE_DIR}/src
         ${CMAKE_SOURCE_DIR}/external
         ${CMAKE_SOURCE_DIR}/external/nvImageCodec/include
-        ${CMAKE_SOURCE_DIR}/external/spz
 )
-
-# Find the actual WebP library targets
-if(TARGET WebP::webpdecoder)
-    set(WEBP_DECODE_LIB WebP::webpdecoder)
-elseif(TARGET WebP::webp)
-    set(WEBP_DECODE_LIB WebP::webp)
-elseif(TARGET webpdecoder)
-    set(WEBP_DECODE_LIB webpdecoder)
-else()
-    find_library(WEBP_DECODE_LIBRARY NAMES webpdecoder webp libwebpdecoder libwebp REQUIRED)
-    set(WEBP_DECODE_LIB ${WEBP_DECODE_LIBRARY})
+if(LFS_ENABLE_SOG_IO)
+    list(APPEND LFS_IO_PRIVATE_INCLUDE_DIRS ${CMAKE_SOURCE_DIR}/external/spz)
 endif()
 
-# WebP encoder for SOG/HTML export
-if(TARGET WebP::webp)
-    set(WEBP_ENCODE_LIB WebP::webp)
-elseif(TARGET webp)
-    set(WEBP_ENCODE_LIB webp)
-else()
-    find_library(WEBP_ENCODE_LIBRARY NAMES webp libwebp REQUIRED)
-    set(WEBP_ENCODE_LIB ${WEBP_ENCODE_LIBRARY})
-endif()
+target_include_directories(lfs_io
+        PUBLIC
+        $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/include>
+        $<INSTALL_INTERFACE:include>
+        PRIVATE
+        ${LFS_IO_PRIVATE_INCLUDE_DIRS}
+)
 
-# Find the actual LibArchive library target
-if(TARGET LibArchive::LibArchive)
-    set(ARCHIVE_LIB LibArchive::LibArchive)
-elseif(TARGET archive)
-    set(ARCHIVE_LIB archive)
-elseif(TARGET archive_static)
-    set(ARCHIVE_LIB archive_static)
-else()
-    find_library(ARCHIVE_LIBRARY NAMES archive libarchive REQUIRED)
-    set(ARCHIVE_LIB ${ARCHIVE_LIBRARY})
+# Find the actual WebP library targets (only if SOG support is enabled)
+if(LFS_ENABLE_SOG_IO)
+    if(TARGET WebP::webpdecoder)
+        set(WEBP_DECODE_LIB WebP::webpdecoder)
+    elseif(TARGET WebP::webp)
+        set(WEBP_DECODE_LIB WebP::webp)
+    elseif(TARGET webpdecoder)
+        set(WEBP_DECODE_LIB webpdecoder)
+    else()
+        find_library(WEBP_DECODE_LIBRARY NAMES webpdecoder webp libwebpdecoder libwebp REQUIRED)
+        set(WEBP_DECODE_LIB ${WEBP_DECODE_LIBRARY})
+    endif()
+
+    # WebP encoder for SOG/HTML export
+    if(TARGET WebP::webp)
+        set(WEBP_ENCODE_LIB WebP::webp)
+    elseif(TARGET webp)
+        set(WEBP_ENCODE_LIB webp)
+    else()
+        find_library(WEBP_ENCODE_LIBRARY NAMES webp libwebp REQUIRED)
+        set(WEBP_ENCODE_LIB ${WEBP_ENCODE_LIBRARY})
+    endif()
+
+    # Find the actual LibArchive library target
+    if(TARGET LibArchive::LibArchive)
+        set(ARCHIVE_LIB LibArchive::LibArchive)
+    elseif(TARGET archive)
+        set(ARCHIVE_LIB archive)
+    elseif(TARGET archive_static)
+        set(ARCHIVE_LIB archive_static)
+    else()
+        find_library(ARCHIVE_LIBRARY NAMES archive libarchive REQUIRED)
+        set(ARCHIVE_LIB ${ARCHIVE_LIBRARY})
+    endif()
 endif()
 
 # Ensure CUDA library is built first
 add_dependencies(lfs_io lfs_io_cuda)
 
+# Build optional link-dep list
+set(LFS_IO_OPTIONAL_LINK_LIBS)
+if(LFS_ENABLE_SOG_IO)
+    list(APPEND LFS_IO_OPTIONAL_LINK_LIBS
+        ${WEBP_DECODE_LIB}     # WebP for SOG decompression
+        ${WEBP_ENCODE_LIB}     # WebP for SOG/HTML export
+        ${ARCHIVE_LIB}         # LibArchive for SOG bundle support
+        spz_lib                # Niantic SPZ compressed format
+    )
+endif()
+if(LFS_ENABLE_USD_IO)
+    list(APPEND LFS_IO_OPTIONAL_LINK_LIBS usdVol)
+endif()
+if(LFS_ENABLE_MESH_IO)
+    list(APPEND LFS_IO_OPTIONAL_LINK_LIBS
+        assimp::assimp
+        $<IF:$<PLATFORM_ID:Windows>,OpenMeshCore,OpenMeshCoreStatic>
+    )
+endif()
+
 # Link dependencies
 target_link_libraries(lfs_io
         PUBLIC
@@ -235,18 +290,26 @@ target_link_libraries(lfs_io
         TBB::tbb
         spdlog::spdlog
         nlohmann_json::nlohmann_json
-        ${WEBP_DECODE_LIB}     # WebP for SOG decompression
-        ${WEBP_ENCODE_LIB}     # WebP for SOG/HTML export
-        ${ARCHIVE_LIB}         # LibArchive for SOG bundle support
         CUDA::cudart           # CUDA runtime for nvImageCodec
         lfs_io_cuda            # IO-specific CUDA kernels (morton, k-means)
         lfs_core_cuda          # Core CUDA utilities (lanczos_resize for nvcodec)
-        assimp::assimp         # Multi-format mesh import
-        usdVol                 # OpenUSD Gaussian ParticleField import
-        $<IF:$<PLATFORM_ID:Windows>,OpenMeshCore,OpenMeshCoreStatic>
-        spz_lib                # Niantic SPZ compressed format
+        ${LFS_IO_OPTIONAL_LINK_LIBS}
 )
 
+# Propagate compile definitions to lfs_io so source code can check
+if(NOT LFS_ENABLE_USD_IO)
+    target_compile_definitions(lfs_io PRIVATE LFS_WITHOUT_USD)
+endif()
+if(NOT LFS_ENABLE_MESH_IO)
+    target_compile_definitions(lfs_io PRIVATE LFS_WITHOUT_MESH)
+endif()
+if(NOT LFS_ENABLE_VIDEO_IO)
+    target_compile_definitions(lfs_io PRIVATE LFS_WITHOUT_VIDEO)
+endif()
+if(NOT LFS_ENABLE_SOG_IO)
+    target_compile_definitions(lfs_io PRIVATE LFS_WITHOUT_SOG)
+endif()
+
 # Link nvImageCodec if available
 if(TARGET nvimgcodec)
     target_link_libraries(lfs_io PRIVATE nvimgcodec)
diff --git a/src/io/loader_service.cpp b/src/io/loader_service.cpp
index ad03fd8a..2aa426df 100644
--- a/src/io/loader_service.cpp
+++ b/src/io/loader_service.cpp
@@ -9,11 +9,17 @@
 #include "io/loaders/blender_loader.hpp"
 #include "io/loaders/checkpoint_loader.hpp"
 #include "io/loaders/colmap_loader.hpp"
-#include "io/loaders/mesh_loader.hpp"
 #include "io/loaders/ply_loader.hpp"
+#ifndef LFS_WITHOUT_MESH
+#include "io/loaders/mesh_loader.hpp"
+#endif
+#ifndef LFS_WITHOUT_SOG
 #include "io/loaders/sogs_loader.hpp"
 #include "io/loaders/spz_loader.hpp"
+#endif
+#ifndef LFS_WITHOUT_USD
 #include "io/loaders/usd_loader.hpp"
+#endif
 #include <format>
 
 namespace lfs::io {
@@ -23,13 +29,19 @@ namespace lfs::io {
 
         // Register default loaders
         registry_->registerLoader(std::make_unique<PLYLoader>());
+#ifndef LFS_WITHOUT_SOG
         registry_->registerLoader(std::make_unique<SogLoader>());
         registry_->registerLoader(std::make_unique<SpzLoader>());
+#endif
+#ifndef LFS_WITHOUT_USD
         registry_->registerLoader(std::make_unique<USDLoader>());
+#endif
         registry_->registerLoader(std::make_unique<CheckpointLoader>());
         registry_->registerLoader(std::make_unique<ColmapLoader>());
         registry_->registerLoader(std::make_unique<BlenderLoader>());
+#ifndef LFS_WITHOUT_MESH
         registry_->registerLoader(std::make_unique<MeshLoader>());
+#endif
 
         LOG_DEBUG("LoaderService initialized with {} loaders", registry_->size());
     }
diff --git a/src/python/CMakeLists.txt b/src/python/CMakeLists.txt
index d98c5a63..7a1d40c3 100644
--- a/src/python/CMakeLists.txt
+++ b/src/python/CMakeLists.txt
@@ -124,11 +124,8 @@ set_target_properties(lfs_python_utils PROPERTIES
 )
 
 # nanobind module (Python import name: lichtfeld)
-# NOTE: We intentionally do NOT use STABLE_ABI here because:
-# 1. The main app embeds Python and links to python312.dll
-# 2. STABLE_ABI would make the .pyd link to python3.dll instead
-# 3. On Windows, having both DLLs loaded causes double Python initialization
-# 4. This makes the .pyd version-specific but avoids the DLL conflict
+# Only built for GUI mode — depends on imgui, implot, RmlUi, and the visualizer.
+if(NOT LFS_BUILD_HEADLESS_ONLY)
 nanobind_add_module(
     lfs_py
     NB_STATIC
@@ -285,10 +282,11 @@ if(WIN32)
         )
     endif()
 endif()
+endif() # NOT LFS_BUILD_HEADLESS_ONLY
 
 # Type stubs for IDE support (output to typings/ to avoid runtime conflicts)
 option(BUILD_PYTHON_STUBS "Generate Python type stubs (requires loading the module)" ON)
-if(BUILD_PYTHON_STUBS)
+if(BUILD_PYTHON_STUBS AND NOT LFS_BUILD_HEADLESS_ONLY)
     set(GENERATED_STUBS_DIR "${CMAKE_CURRENT_BINARY_DIR}/typings")
     set(COMMITTED_STUBS_DIR "${CMAKE_CURRENT_SOURCE_DIR}/stubs")
     set(PYTHON_STUB_WORKFLOW_SCRIPT "${CMAKE_CURRENT_SOURCE_DIR}/scripts/manage_stubs.py")
@@ -411,7 +409,9 @@ add_custom_target(lfs_python_support_copy ALL
     COMMENT "Copying internal Python support modules"
 )
 
-add_dependencies(lfs_py lfs_plugins_copy lfs_python_support_copy)
+if(TARGET lfs_py)
+    add_dependencies(lfs_py lfs_plugins_copy lfs_python_support_copy)
+endif()
 if(TARGET lichtfeld_stub)
     add_dependencies(lichtfeld_stub lfs_python_support_copy)
 endif()
@@ -431,16 +431,18 @@ install_uv()
 set(PYTHON_INSTALL_DIR "${CMAKE_INSTALL_LIBDIR}/python")
 
 # Install lichtfeld Python module (.so/.pyd) to bin for portable builds
-if(BUILD_PORTABLE)
-    install(TARGETS lfs_py
-        LIBRARY DESTINATION "${CMAKE_INSTALL_BINDIR}"
-        RUNTIME DESTINATION "${CMAKE_INSTALL_BINDIR}"  # Windows DLLs
-    )
-else()
-    install(TARGETS lfs_py
-        LIBRARY DESTINATION "${PYTHON_INSTALL_DIR}"
-        RUNTIME DESTINATION "${PYTHON_INSTALL_DIR}"  # Windows DLLs
-    )
+if(TARGET lfs_py)
+    if(BUILD_PORTABLE)
+        install(TARGETS lfs_py
+            LIBRARY DESTINATION "${CMAKE_INSTALL_BINDIR}"
+            RUNTIME DESTINATION "${CMAKE_INSTALL_BINDIR}"  # Windows DLLs
+        )
+    else()
+        install(TARGETS lfs_py
+            LIBRARY DESTINATION "${PYTHON_INSTALL_DIR}"
+            RUNTIME DESTINATION "${PYTHON_INSTALL_DIR}"  # Windows DLLs
+        )
+    endif()
 endif()
 
 # Install lfs_python_runtime shared library (required by lichtfeld module and exe)
diff --git a/vcpkg.json b/vcpkg.json
index 3c5a6eb2..5f1bffdd 100644
--- a/vcpkg.json
+++ b/vcpkg.json
@@ -4,34 +4,8 @@
     "args",
     "boost-preprocessor",
     "boost-regex",
-    {
-      "name": "ffmpeg",
-      "features": [
-        "avcodec",
-        "avformat",
-        "swscale",
-        "nvcodec",
-        "x264"
-      ]
-    },
-    "freetype",
-    "glad",
-    "sdl3",
     "glm",
     "gtest",
-    {
-      "name": "imgui",
-      "features": [
-        "docking-experimental",
-        "freetype",
-        "opengl3-binding",
-        "sdl3-binding"
-      ]
-    },
-    "implot",
-    "libarchive",
-    "libwebp",
-    "nativefiledialog-extended",
     "nlohmann-json",
     {
       "name": "openimageio",
@@ -41,20 +15,74 @@
     },
     "spdlog",
     "tbb",
-    "usd",
     {
       "name": "python3",
       "features": [
         "extensions"
       ]
     },
-    "nanobind",
-    "assimp",
-    {
-      "name": "rmlui",
-      "features": [
-        "svg"
+    "nanobind"
+  ],
+  "features": {
+    "gui": {
+      "description": "GUI / visualizer dependencies (SDL3, imgui, Freetype, RmlUi, ...)",
+      "dependencies": [
+        "freetype",
+        "glad",
+        "sdl3",
+        {
+          "name": "imgui",
+          "features": [
+            "docking-experimental",
+            "freetype",
+            "opengl3-binding",
+            "sdl3-binding"
+          ]
+        },
+        "implot",
+        "nativefiledialog-extended",
+        {
+          "name": "rmlui",
+          "features": [
+            "svg"
+          ]
+        }
+      ]
+    },
+    "video": {
+      "description": "FFmpeg video encoder / player",
+      "dependencies": [
+        {
+          "name": "ffmpeg",
+          "features": [
+            "avcodec",
+            "avformat",
+            "swscale",
+            "nvcodec",
+            "x264"
+          ]
+        }
+      ]
+    },
+    "usd": {
+      "description": "OpenUSD Gaussian ParticleField import",
+      "dependencies": [
+        "usd"
+      ]
+    },
+    "mesh": {
+      "description": "Mesh import (assimp + OpenMesh)",
+      "dependencies": [
+        "assimp"
+      ]
+    },
+    "sog": {
+      "description": "SOG / SPZ / HTML export (WebP + LibArchive)",
+      "dependencies": [
+        "libarchive",
+        "libwebp"
       ]
     }
-  ]
+  },
+  "default-features": ["gui", "video", "usd", "mesh", "sog"]
 }
