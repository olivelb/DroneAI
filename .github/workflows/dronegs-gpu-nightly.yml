name: DroneGS GPU nightly

on:
  schedule:
    - cron: "17 2 * * *"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: dronegs-gpu-nightly
  cancel-in-progress: true

jobs:
  cuda-tests:
    if: vars.DRONEGS_GPU_CI == 'true'
    runs-on: [self-hosted, linux, x64, gpu, cuda]
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1; runner >= 2.327.1
      - name: Run native CUDA suites in the development container
        run: scripts/ci/validate_cuda_containers.sh gpu
